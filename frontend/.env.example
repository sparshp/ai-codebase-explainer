# Vite env — copy to .env.local for production builds

# Dev: leave unset (uses Vite proxy /api → localhost backend)
# Production: your Render API URL, e.g. https://codeexplainer-api.onrender.com
VITE_API_BASE_URL=
